% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiLLMviaionet.R
\name{multiLLM_random10}
\alias{multiLLM_random10}
\title{multiLLM_random10: Quick execution of 10 randomly selected models}
\usage{
multiLLM_random10(
  prompt,
  balanced = TRUE,
  exclude_models = character(0),
  api_key = Sys.getenv("IONET_API_KEY"),
  max_tokens = 1024,
  temperature = 0.7,
  timeout = 300,
  streaming = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{prompt}{A string containing the input prompt to send to all models.}

\item{balanced}{Logical indicating whether to balance selection across model categories.
If TRUE (default), selects models from different families. If FALSE, uses pure random selection.}

\item{exclude_models}{Character vector of model names to exclude from random selection.}

\item{api_key}{A string containing the io.net API key. 
Defaults to the environment variable "IONET_API_KEY".}

\item{max_tokens}{Integer specifying maximum tokens to generate per model. Default is 1024.}

\item{temperature}{Numeric value controlling randomness (0-2). Default is 0.7.}

\item{timeout}{Numeric value in seconds for request timeout per model. Default is 300.}

\item{streaming}{Logical indicating whether to use streaming responses. Default is TRUE.}

\item{verbose}{Logical indicating whether to show detailed progress. Default is TRUE.}
}
\value{
A multiLLM_result object with results from 10 randomly selected models.
}
\description{
Convenience function that automatically selects 10 diverse models 
  from different categories and executes them in parallel. This provides a 
  comprehensive comparison across model families without manual selection.
}
\details{
Random 10 Model Multi-LLM Execution
}
\examples{
\dontrun{
  # Quick 10-model comparison with balanced selection
  result <- multiLLM_random10(
    prompt = "Explain the benefits of renewable energy"
  )
  
  # Pure random selection
  result <- multiLLM_random10(
    prompt = "Write a haiku about technology",
    balanced = FALSE,
    temperature = 1.0
  )
  
  # Exclude specific models
  result <- multiLLM_random10(
    prompt = "Solve this math problem: 2x + 5 = 15",
    exclude_models = c("microsoft/Phi-3.5-mini-instruct"),
    temperature = 0.1
  )
}
}
\author{
Satoshi Kume
}
